diff --git a/node_modules/expo-dynamic-app-icon/android/build.gradle b/node_modules/expo-dynamic-app-icon/android/build.gradle
index a50832e..a3bf5b9 100644
--- a/node_modules/expo-dynamic-app-icon/android/build.gradle
+++ b/node_modules/expo-dynamic-app-icon/android/build.gradle
@@ -55,12 +55,12 @@ android {
   compileSdkVersion safeExtGet("compileSdkVersion", 31)
 
   compileOptions {
-    sourceCompatibility JavaVersion.VERSION_11
-    targetCompatibility JavaVersion.VERSION_11
+    sourceCompatibility JavaVersion.VERSION_17
+    targetCompatibility JavaVersion.VERSION_17
   }
 
   kotlinOptions {
-    jvmTarget = JavaVersion.VERSION_11.majorVersion
+    jvmTarget = JavaVersion.VERSION_17.majorVersion
   }
 
   defaultConfig {
diff --git a/node_modules/expo-dynamic-app-icon/ios/ExpoDynamicAppIconModule.swift b/node_modules/expo-dynamic-app-icon/ios/ExpoDynamicAppIconModule.swift
index 429bfe2..f37c42b 100644
--- a/node_modules/expo-dynamic-app-icon/ios/ExpoDynamicAppIconModule.swift
+++ b/node_modules/expo-dynamic-app-icon/ios/ExpoDynamicAppIconModule.swift
@@ -6,7 +6,11 @@ public class ExpoDynamicAppIconModule: Module {
     Name("ExpoDynamicAppIcon")
 
     Function("setAppIcon") { (name:String) -> String in
-      self.setAppIconWithoutAlert(name)
+      // An empty name means "reset to the primary icon" — the private API
+      // requires nil (not the string "DEFAULT"/"") to actually clear the
+      // alternate icon, otherwise it silently no-ops looking for an icon
+      // literally named that.
+      self.setAppIconWithoutAlert(name.isEmpty ? nil : name)
 
       return name
     }
diff --git a/node_modules/expo-dynamic-app-icon/plugin/src/withDynamicIcon.ts b/node_modules/expo-dynamic-app-icon/plugin/src/withDynamicIcon.ts
index 35dffdd..768ff3b 100644
--- a/node_modules/expo-dynamic-app-icon/plugin/src/withDynamicIcon.ts
+++ b/node_modules/expo-dynamic-app-icon/plugin/src/withDynamicIcon.ts
@@ -82,6 +82,24 @@ const withIconAndroidManifest: ConfigPlugin<Props> = (config, { icons }) => {
     const iconNamePrefix = `${config.android!.package}.MainActivity`;
     const iconNames = Object.keys(icons);
 
+    // Only one of MainActivity / an icon alias is ever enabled at a time —
+    // whichever one is disabled can't resolve intents targeting it. Without
+    // copying MainActivity's own VIEW intent-filters (deep links, the Expo
+    // dev-client relaunch scheme) onto every alias too, switching to an
+    // alternate icon silently breaks deep linking and dev-client resume.
+    const mainActivity = (mainApplication.activity || []).find(
+      (activity: any) => activity.$["android:name"] === ".MainActivity",
+    );
+    const deepLinkIntentFilters = (
+      (mainActivity && mainActivity["intent-filter"]) ||
+      []
+    ).filter((filter: any) =>
+      (filter.action || []).some(
+        (action: any) =>
+          action.$["android:name"] === "android.intent.action.VIEW",
+      ),
+    );
+
     function addIconActivityAlias(config: any[]): any[] {
       return [
         ...config,
@@ -100,6 +118,7 @@ const withIconAndroidManifest: ConfigPlugin<Props> = (config, { icons }) => {
                 { $: { "android:name": "android.intent.category.LAUNCHER" } },
               ],
             },
+            ...deepLinkIntentFilters,
           ],
         })),
       ];
diff --git a/node_modules/expo-dynamic-app-icon/plugin/build/withDynamicIcon.js b/node_modules/expo-dynamic-app-icon/plugin/build/withDynamicIcon.js
index a416495..a54e8fb 100644
--- a/node_modules/expo-dynamic-app-icon/plugin/build/withDynamicIcon.js
+++ b/node_modules/expo-dynamic-app-icon/plugin/build/withDynamicIcon.js
@@ -49,6 +49,13 @@ const withIconAndroidManifest = (config, { icons }) => {
         const mainApplication = getMainApplicationOrThrow(config.modResults);
         const iconNamePrefix = `${config.android.package}.MainActivity`;
         const iconNames = Object.keys(icons);
+        // Only one of MainActivity / an icon alias is ever enabled at a time —
+        // whichever one is disabled can't resolve intents targeting it. Without
+        // copying MainActivity's own VIEW intent-filters (deep links, the Expo
+        // dev-client relaunch scheme) onto every alias too, switching to an
+        // alternate icon silently breaks deep linking and dev-client resume.
+        const mainActivity = (mainApplication.activity || []).find((activity) => activity.$["android:name"] === ".MainActivity");
+        const deepLinkIntentFilters = ((mainActivity && mainActivity["intent-filter"]) || []).filter((filter) => (filter.action || []).some((action) => action.$["android:name"] === "android.intent.action.VIEW"));
         function addIconActivityAlias(config) {
             return [
                 ...config,
@@ -67,6 +74,7 @@ const withIconAndroidManifest = (config, { icons }) => {
                                 { $: { "android:name": "android.intent.category.LAUNCHER" } },
                             ],
                         },
+                        ...deepLinkIntentFilters,
                     ],
                 })),
             ];
